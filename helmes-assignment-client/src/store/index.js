import { createStore } from 'vuex'
import exception from "@/store/exception";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    exception
  }
})
